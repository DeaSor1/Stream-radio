# 🎵 Automated Radio Station

Автоматическая радиостанция на базе Liquidsoap и Python для Android (Termux).

## 🚀 Быстрый старт

### На компьютере (для разработки)
```bash
./start_radio.sh
```

### В Termux (Android)
```bash
# 1. Клонировать репозиторий
git clone <your-repo-url>
cd Stream-radio

# 2. Запустить радио (все зависимости установятся автоматически)
bash start_radio.sh
```

## 📁 Структура проекта

```
Stream-radio/
├── config/
│   ├── icecast.xml          # Конфигурация Icecast сервера
│   └── station.liq          # Конфигурация Liquidsoap (обработка аудио)
├── scripts/
│   ├── playlist_manager.py  # Управление плейлистом
│   └── normalize_music.py   # Нормализация громкости файлов
├── music/                   # Папка с музыкой (MP3, FLAC)
├── logs/                    # Логи серверов
├── data/                    # Данные приложения
├── requirements.txt         # Python зависимости
└── start_radio.sh          # Скрипт запуска
```

## 🎚️ Проблема с громкостью?

Если музыка то тихая, то громкая, есть два решения:

### Решение 1: Текущие настройки (уже применено)
В `config/station.liq` используется:
- **Компрессор** - сглаживает разницу между тихими и громкими частями
- **Лимитер** - предотвращает искажения

### Решение 2: Предварительная нормализация (рекомендуется)
Нормализуйте все файлы один раз:

```bash
source .venv/bin/activate
python scripts/normalize_music.py
```

Это приведёт все треки к одинаковой громкости (-16 LUFS стандарт).

## 🔧 Требования

### Системные пакеты
- `liquidsoap` - аудио движок
- `icecast` - стриминг сервер
- `ffmpeg` - кодирование аудио
- `python3` - для скриптов

### Python пакеты (устанавливаются автоматически)
- `mutagen` - работа с метаданными
- `ffmpeg-normalize` - нормализация громкости
- `tqdm` - прогресс-бары

## 📡 Подключение к радио

После запуска радио доступно по адресу:
```
http://localhost:8000/radio
```

В плеере (VLC, mpv, браузер) открыть этот URL.

## 🛠️ Обновление через Git

```bash
# Получить последние изменения
git pull

# Запустить (зависимости обновятся автоматически)
bash start_radio.sh
```

## 📝 Примечания

- Скрипт автоматически устанавливает Python зависимости при каждом запуске
- Создаются резервные копии при нормализации (файлы `.backup`)
- FLAC файлы требуют дополнительных кодеков в Liquidsoap

## 🐛 Troubleshooting

### Радио не запускается
```bash
# Проверить логи
cat logs/icecast_startup.log

# Убить зависшие процессы
pkill -f liquidsoap
pkill -f icecast
```

### Музыка не играет
- Проверьте, есть ли файлы в папке `music/`
- Убедитесь, что файлы в формате MP3 или FLAC
- Проверьте права доступа к файлам

---

**Автор:** DeaSor  
**Лицензия:** MIT
